Issues fixed in code:
1) added List import
2) changed function header to def sort_list(unsorted_list: List):
3) fixed indentation issues
4) fixed closed bracket issues
5) fixed if __name__ == "__name__" to if __name == "__main__"
